export interface ContentfulResources {
  fields: {
    title: string
    description: string
    contactItems: string
  }
}

export interface ContentfulSchedules {
  fields: {
    title: string
    days: string
    location: string
    pdfLink: string
  }
}
